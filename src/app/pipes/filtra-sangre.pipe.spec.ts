import { FiltraSangrePipe } from './filtra-sangre.pipe';

describe('FiltraSangrePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltraSangrePipe();
    expect(pipe).toBeTruthy();
  });
});
